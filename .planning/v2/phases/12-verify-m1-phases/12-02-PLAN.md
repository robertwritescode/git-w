---
phase: 12-verify-m1-phases
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-add-remote-and-remote-branch-rule/04-VERIFICATION.md
  - .planning/phases/05-add-sync-pair-parsing/05-VERIFICATION.md
autonomous: true
requirements:
  - CFG-04
  - CFG-05

must_haves:
  truths:
    - "Phase 04 VERIFICATION.md exists with status passed/gaps_found"
    - "Phase 05 VERIFICATION.md exists with status passed/gaps_found"
    - "CFG-04 requirement coverage assessed in Phase 04 report"
    - "CFG-05 requirement coverage assessed in Phase 05 report"
  artifacts:
    - path: ".planning/phases/04-add-remote-and-remote-branch-rule/04-VERIFICATION.md"
      provides: "Phase 04 verification report covering CFG-04"
      contains: "status:"
    - path: ".planning/phases/05-add-sync-pair-parsing/05-VERIFICATION.md"
      provides: "Phase 05 verification report covering CFG-05"
      contains: "status:"
  key_links:
    - from: ".planning/phases/04-add-remote-and-remote-branch-rule/04-01-SUMMARY.md"
      to: ".planning/phases/04-add-remote-and-remote-branch-rule/04-VERIFICATION.md"
      via: "verification reads summaries and verifies artifacts against codebase"
      pattern: "VERIFICATION"
    - from: ".planning/phases/05-add-sync-pair-parsing/05-01-SUMMARY.md"
      to: ".planning/phases/05-add-sync-pair-parsing/05-VERIFICATION.md"
      via: "verification reads summaries and verifies artifacts against codebase"
      pattern: "VERIFICATION"
---

<objective>
Generate VERIFICATION.md reports for Phase 04 (remote + branch_rule parsing) and Phase 05 (sync_pair with cycle detection) by checking each phase's implemented artifacts against its ROADMAP success criteria.

Purpose: M1 phases were implemented but never formally verified. This plan closes that gap for Phases 04 and 05.

Output: Two VERIFICATION.md files documenting the verification status of each phase's goal.
</objective>

<execution_context>
@/Users/robert/code/robertwritescode/git-w/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/robert/code/robertwritescode/git-w/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-add-remote-and-remote-branch-rule/04-01-SUMMARY.md
@.planning/phases/04-add-remote-and-remote-branch-rule/04-02-SUMMARY.md
@.planning/phases/05-add-sync-pair-parsing/05-01-SUMMARY.md
@.planning/phases/05-add-sync-pair-parsing/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 04 — [[remote]] and [[remote.branch_rule]] parsing</name>
  <read_first>
    - .planning/phases/04-add-remote-and-remote-branch-rule/04-01-SUMMARY.md
    - .planning/phases/04-add-remote-and-remote-branch-rule/04-02-SUMMARY.md
    - .planning/ROADMAP.md (Phase 4 section: success criteria, requirement CFG-04)
    - pkg/config/config.go (RemoteConfig, BranchRuleConfig, BranchAction, MergeRemote)
    - pkg/config/loader.go (validateRemotes, diskConfig.RemoteList)
    - pkg/config/loader_test.go (remote-related tests)
  </read_first>
  <action>
    Verify Phase 04 against ROADMAP success criteria:
    1. `[[remote]]` blocks parse all specified fields (name, url, type, token_env, repo_prefix, repo_suffix, push_mode, critical)
    2. `[[remote.branch_rule]]` sub-blocks parse pattern, action, criteria fields
    3. Branch rules preserve declaration order (array-of-tables, not map)
    4. Invalid remote or rule configurations produce actionable validation errors

    **Verification steps:**
    1. Read both SUMMARY.md files to understand what was implemented
    2. Run: `grep -n "RemoteConfig\|BranchRuleConfig\|BranchAction" pkg/config/config.go | head -30`
    3. Run: `grep -n "push_mode\|token_env\|repo_prefix\|repo_suffix\|critical" pkg/config/config.go`
    4. Run: `grep -n "validateRemotes\|RemoteList\|RemoteByName" pkg/config/loader.go | head -20`
    5. Run: `grep -n "TestRemote\|TestBranchRule" pkg/config/loader_test.go | head -20`
    6. Run: `mage testfast 2>&1 | grep "config\|FAIL"`
    7. Verify `[[remote.branch_rule]]` uses slice (order-preserving), not map: `grep -n "BranchRules\|branch_rule" pkg/config/config.go`

    **Write** `.planning/phases/04-add-remote-and-remote-branch-rule/04-VERIFICATION.md` using the template at `.opencode/get-shit-done/templates/verification-report.md`.

    Use the must_haves from 04-01-PLAN.md and 04-02-PLAN.md as the truths and artifacts source.

    **Expected status:** `passed` — both plans have SUMMARY.md with completed implementation, tests pass.

    Set frontmatter: `phase: 04-add-remote-and-remote-branch-rule`, `verified: <today's date>T00:00:00Z`, `status: passed` (or `gaps_found` if issues discovered).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-add-remote-and-remote-branch-rule/04-VERIFICATION.md && grep "status:" .planning/phases/04-add-remote-and-remote-branch-rule/04-VERIFICATION.md</automated>
  </verify>
  <done>
    - 04-VERIFICATION.md exists in .planning/phases/04-add-remote-and-remote-branch-rule/
    - File contains valid frontmatter with status field
    - All 4 ROADMAP success criteria assessed
    - CFG-04 requirement coverage section present
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 05 — [[sync_pair]] with cycle detection</name>
  <read_first>
    - .planning/phases/05-add-sync-pair-parsing/05-01-SUMMARY.md
    - .planning/phases/05-add-sync-pair-parsing/05-02-SUMMARY.md
    - .planning/ROADMAP.md (Phase 5 section: success criteria, requirement CFG-05)
    - pkg/config/config.go (SyncPairConfig, SyncPairs, MergeSyncPair)
    - pkg/config/loader.go (validateSyncPairFields, detectSyncCycles, diskConfig.SyncPairList)
    - pkg/config/loader_test.go (TestSyncPair*, TestSyncCycleDetection)
  </read_first>
  <action>
    Verify Phase 05 against ROADMAP success criteria:
    1. `[[sync_pair]]` blocks parse source, destination, and ref_patterns fields
    2. Cycle detection at load time identifies circular sync routes
    3. Cycles produce actionable error message naming the cycle path

    **Verification steps:**
    1. Read both SUMMARY.md files
    2. Run: `grep -n "SyncPairConfig\|SyncPairs\|MergeSyncPair" pkg/config/config.go`
    3. Run: `grep -n "From\|To\|Refs\|toml:" pkg/config/config.go | grep -A2 -B2 "SyncPair"`
    4. Run: `grep -n "validateSyncPairFields\|detectSyncCycles\|dfsSyncCycle\|SyncPairList" pkg/config/loader.go`
    5. Run: `grep -n "TestSyncPair\|TestSyncCycle" pkg/config/loader_test.go`
    6. Run: `mage testfast 2>&1 | grep "config\|FAIL"`
    7. Verify error format for cycles: `grep -n "cycle detected" pkg/config/loader.go`
      — should contain `"sync_pair cycle detected: origin → personal → origin"` pattern

    **Note on field naming:** ROADMAP says "source, destination, and ref_patterns" but SUMMARY.md shows fields named `From`, `To`, `Refs`. Verify the TOML tags match (`from`, `to`, `refs`) — these are equivalent per implementation decision. Note this in the verification report.

    **Write** `.planning/phases/05-add-sync-pair-parsing/05-VERIFICATION.md` using the template.

    **Expected status:** `passed` — both plans have SUMMARY.md with completed implementation, tests pass.

    Set frontmatter: `phase: 05-add-sync-pair-parsing`, `verified: <today's date>T00:00:00Z`, `status: passed` (or `gaps_found` if issues discovered).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-add-sync-pair-parsing/05-VERIFICATION.md && grep "status:" .planning/phases/05-add-sync-pair-parsing/05-VERIFICATION.md</automated>
  </verify>
  <done>
    - 05-VERIFICATION.md exists in .planning/phases/05-add-sync-pair-parsing/
    - File contains valid frontmatter with status field
    - All 3 ROADMAP success criteria assessed
    - CFG-05 requirement coverage section present
    - Field naming discrepancy (From/To vs source/destination) documented
  </done>
</task>

</tasks>

<verification>
```bash
test -f .planning/phases/04-add-remote-and-remote-branch-rule/04-VERIFICATION.md && echo "04 ✓" || echo "04 ✗"
test -f .planning/phases/05-add-sync-pair-parsing/05-VERIFICATION.md && echo "05 ✓" || echo "05 ✗"
grep "status:" .planning/phases/04-add-remote-and-remote-branch-rule/04-VERIFICATION.md
grep "status:" .planning/phases/05-add-sync-pair-parsing/05-VERIFICATION.md
```
Both files exist with valid status fields.
</verification>

<success_criteria>
- 04-VERIFICATION.md exists covering CFG-04
- 05-VERIFICATION.md exists covering CFG-05
- Both files have valid frontmatter (phase, verified, status, score)
- Both cover all ROADMAP success criteria for their respective phase
- Phase 05 notes From/To/Refs field naming vs ROADMAP's source/destination/ref_patterns
</success_criteria>

<output>
After completion, create `.planning/phases/12-verify-m1-phases/12-02-SUMMARY.md`
</output>
