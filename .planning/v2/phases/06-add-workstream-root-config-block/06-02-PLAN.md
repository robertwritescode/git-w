---
phase: 06-add-workstream-root-config-block
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - pkg/config/loader.go
  - pkg/config/loader_test.go
autonomous: true
requirements:
  - CFG-06
must_haves:
  truths:
    - "`[[workstream]]` blocks in `.gitw` parse `name` and `remotes`"
    - "Workstream remote references are validated against declared `[[remote]]` names"
    - "Invalid workstream shapes (missing keys, unknown keys, duplicates) fail load with actionable errors"
    - "Loaded workstreams and remote lists are normalized into sorted order"
  artifacts:
    - path: "pkg/config/loader.go"
      provides: "diskConfig wiring, validateWorkstreams, strict-key checks, normalization"
      contains: "validateWorkstreams"
    - path: "pkg/config/loader_test.go"
      provides: "parse, validation, strict-key, and normalization tests for [[workstream]]"
      contains: "TestWorkstreamBlocksParse"
  key_links:
    - from: "pkg/config/loader.go"
      to: "cfg.Remotes"
      via: "validateWorkstreams remote reference checks"
      pattern: "unknown remote"
    - from: "pkg/config/loader.go"
      to: "cfg.Workstreams"
      via: "buildAndValidate validation + normalization"
      pattern: "sort\.Slice\(cfg\.Workstreams"
---

<objective>
Wire `[[workstream]]` into loader parse/validate/normalize flow so Phase 6 CFG-06 behaviors are fully enforced.

Purpose: Delivers parse + immediate validation guarantees and deterministic normalization for root workstream overrides.
Output: loader wiring, strict validation, and full `loader_test.go` coverage for allowed files, required keys, strict keys, duplicates, and remote-reference checks.
</objective>

<execution_context>
@/Users/robert/code/robertwritescode/git-w/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/robert/code/robertwritescode/git-w/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-workstream-root-config-block/06-CONTEXT.md
@.planning/phases/06-add-workstream-root-config-block/06-RESEARCH.md
@.planning/v2/v2-schema.md
@.planning/v2/v2-remote-management.md

<interfaces>
From `pkg/config/loader.go` current chain:

```go
func buildAndValidate(configPath string, cfg *WorkspaceConfig) error {
    ...
    if err := validateRemotes(configPath, cfg); err != nil { return err }
    if err := validateSyncPairFields(cfg); err != nil { return err }
    if err := detectSyncCycles(cfg); err != nil { return err }
    return validateAliasFields(cfg)
}

type diskConfig struct {
    RemoteList   []RemoteConfig   `toml:"remote,omitempty"`
    SyncPairList []SyncPairConfig `toml:"sync_pair,omitempty"`
}
```

From `pkg/config/config.go` (Plan 06-01 output expected):

```go
type WorkstreamConfig struct {
    Name    string   `toml:"name"`
    Remotes []string `toml:"remotes,omitempty"`
}
```

Validation must honor D-01..D-15 from CONTEXT.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire loader parse + validateWorkstreams + normalization</name>
  <files>pkg/config/loader.go</files>
  <read_first>
    - pkg/config/loader.go
    - pkg/config/config.go
    - .planning/phases/06-add-workstream-root-config-block/06-CONTEXT.md
    - .planning/v2/v2-schema.md
  </read_first>
  <behavior>
    - Test 1: Workstream entries parse from `.gitw` and `.git/.gitw` without placement warnings/errors.
    - Test 2: Missing `name` or missing `remotes` key fails load.
    - Test 3: Unknown key in `[[workstream]]` fails load.
    - Test 4: Unknown remote name and duplicate remotes fail load.
    - Test 5: Workstream list and each remotes list are sorted in-memory.
  </behavior>
  <action>
    Implement loader integration per locked decisions:

    1) Extend `diskConfig` with `WorkstreamList []WorkstreamConfig \`toml:"workstream,omitempty"\``.
    2) In `loadMainConfig`, set `Workstreams: dc.WorkstreamList` on `WorkspaceConfig`.
    3) In `prepareDiskConfig`, include `WorkstreamList: cfg.Workstreams`.
    4) In `buildAndValidate`, after `validateRemotes(configPath, cfg)` and before sync-pair validation, call `validateWorkstreams(configPath, cfg)`.

    Implement `validateWorkstreams(configPath string, cfg *WorkspaceConfig) error` with these concrete checks:
    - D-01/D-02/D-03: do NOT reject `.gitw` placement; no warning path for `.gitw` workstreams.
    - D-08: each entry must have non-empty `Name`; error format includes entry index and missing `name`.
    - D-05: `remotes` key must be present even if empty list. Track key presence and fail if absent.
    - D-09: duplicate workstream names are hard errors.
    - D-10/D-11: every remote in `Remotes` must exist in declared `cfg.Remotes` names.
    - D-12: duplicates inside one `Remotes` list are hard errors.
    - D-13: normalize/sort `cfg.Workstreams` by `Name` ascending.
    - D-14: normalize/sort each `WorkstreamConfig.Remotes` slice ascending.

    Implement strict-key enforcement for D-07:
    - Add a targeted parser/check that inspects each raw `[[workstream]]` table keys and allows only `name` and `remotes`.
    - Return load error naming workstream index and offending key.
    - Do not globally tighten unknown-key behavior for non-workstream blocks.

    Maintain existing code conventions:
    - Guard clauses and short helpers.
    - No new dependencies.
  </action>
  <acceptance_criteria>
    - `pkg/config/loader.go` contains `WorkstreamList []WorkstreamConfig` in `diskConfig`.
    - `pkg/config/loader.go` assigns `cfg.Workstreams` in `loadMainConfig`.
    - `pkg/config/loader.go` includes `WorkstreamList: cfg.Workstreams` in `prepareDiskConfig`.
    - `pkg/config/loader.go` contains `validateWorkstreams(configPath string, cfg *WorkspaceConfig) error`.
    - `buildAndValidate` invokes `validateWorkstreams` between remote and sync-pair validation.
    - Code contains sorting calls for `cfg.Workstreams` and each `workstream.Remotes`.
  </acceptance_criteria>
  <verify>
    <automated>mage testfast</automated>
  </verify>
  <done>Loader fully wires workstream parse/validate/normalize flow with strict schema enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add loader tests for parse, strictness, validation, and ordering</name>
  <files>pkg/config/loader_test.go</files>
  <read_first>
    - pkg/config/loader_test.go
    - pkg/config/loader.go
    - .planning/phases/06-add-workstream-root-config-block/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: valid parse for single/multiple `[[workstream]]` blocks.
    - Test 2: `.gitw` placement accepted without warning/error.
    - Test 3: strict-key, required-key, duplicate, and unknown-reference failures.
    - Test 4: normalized sorting of workstreams/remotes in loaded config.
  </behavior>
  <action>
    Add/extend table-driven tests in `LoaderSuite`:

    1) `TestWorkstreamBlocksParse`:
       - no block => empty `cfg.Workstreams`
       - one block with `name` + `remotes`
       - one block with `remotes = []` (valid per D-05/D-06)
       - multiple blocks parse

    2) `TestWorkstreamValidation` with explicit invalid cases:
       - missing `name`
       - missing `remotes` key (must fail)
       - duplicate workstream name
       - unknown extra key (for example `policy = "x"`) fails strict key check
       - unknown remote reference (`remotes=["does-not-exist"]`)
       - duplicate remote in list (`["origin","origin"]`)

    3) `TestWorkstreamPlacementAllowedInPublicConfig`:
       - config file path `.gitw` with `[[workstream]]` loads successfully
       - no warning text indicating misconfiguration

    4) `TestWorkstreamNormalizationOrder`:
       - unsorted workstream names load sorted
       - unsorted remotes in each workstream load sorted

    Ensure tests assert actionable error fragments for each validation path.
  </action>
  <acceptance_criteria>
    - `pkg/config/loader_test.go` contains `TestWorkstreamBlocksParse`.
    - `pkg/config/loader_test.go` contains `TestWorkstreamValidation`.
    - `pkg/config/loader_test.go` contains `TestWorkstreamPlacementAllowedInPublicConfig`.
    - `pkg/config/loader_test.go` contains `TestWorkstreamNormalizationOrder`.
    - `mage testfast` passes and `mage test` passes.
  </acceptance_criteria>
  <verify>
    <automated>mage test</automated>
  </verify>
  <done>Automated coverage proves all Phase 6 locked decisions and CFG-06 success criteria.</done>
</task>

</tasks>

<verification>
Run:
- `mage testfast`
- `mage test`

Both commands must pass before plan completion.
</verification>

<success_criteria>
Phase 6 loader behavior is complete: `[[workstream]]` parses in root/private configs, strict schema and required-key checks are enforced, remote references validated, duplicates rejected, and in-memory ordering normalized.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-workstream-root-config-block/06-02-SUMMARY.md`.
</output>
