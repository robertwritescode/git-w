---
phase: 06-add-workstream-root-config-block
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/config/config.go
  - pkg/config/config_test.go
autonomous: true
requirements:
  - CFG-06
must_haves:
  truths:
    - "`[[workstream]]` entries have an explicit config type with `name` and `remotes`"
    - "Workstream entries can be looked up and merged in-memory for later cascade use"
    - "Multiple workstream entries can coexist as a slice on WorkspaceConfig"
  artifacts:
    - path: "pkg/config/config.go"
      provides: "WorkstreamConfig type, Workstreams field, MergeWorkstream helper"
      contains: "type WorkstreamConfig struct"
    - path: "pkg/config/config_test.go"
      provides: "Table-driven tests for MergeWorkstream and WorkstreamByName"
      contains: "TestMergeWorkstream"
  key_links:
    - from: "pkg/config/loader.go"
      to: "pkg/config/config.go"
      via: "diskConfig.WorkstreamList -> cfg.Workstreams (Plan 06-02)"
      pattern: "WorkstreamConfig"
---

<objective>
Define the `[[workstream]]` in-memory contract in `pkg/config` so loader and later cascade phases have a stable type to build on.

Purpose: Establishes Phase 6 schema primitives required by CFG-06 before loader wiring and validation.
Output: `WorkstreamConfig`, `WorkspaceConfig.Workstreams`, `MergeWorkstream`, lookup helper, and config-level unit tests.
</objective>

<execution_context>
@/Users/robert/code/robertwritescode/git-w/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/robert/code/robertwritescode/git-w/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-workstream-root-config-block/06-CONTEXT.md
@.planning/phases/06-add-workstream-root-config-block/06-RESEARCH.md

<interfaces>
From `pkg/config/config.go` (existing patterns):

```go
type WorkspaceConfig struct {
    Metarepo   MetarepoConfig
    Workspaces []WorkspaceBlock
    Remotes    []RemoteConfig
    SyncPairs  []SyncPairConfig
}

func MergeRemote(base, override RemoteConfig) RemoteConfig
func MergeSyncPair(base, override SyncPairConfig) SyncPairConfig
func (c *WorkspaceConfig) RemoteByName(name string) (RemoteConfig, bool)
```

Use the same style for workstream contract additions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WorkstreamConfig contract on WorkspaceConfig</name>
  <files>pkg/config/config.go</files>
  <read_first>
    - pkg/config/config.go
    - .planning/phases/06-add-workstream-root-config-block/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `MergeWorkstream` keeps base values when override is zero-value.
    - Test 2: Override `Name` and non-empty `Remotes` replace base values.
    - Test 3: Empty override remotes do not replace base remotes.
    - Test 4: `WorkstreamByName` returns first matching entry and false when missing.
  </behavior>
  <action>
    Implement per D-04 and D-13/D-14 prerequisites:
    1) Add `Workstreams []WorkstreamConfig` to `WorkspaceConfig` directly after `SyncPairs`, with comment `in-memory; populated from [[workstream]] list by loader`.
    2) Add `type WorkstreamConfig struct { Name string \`toml:"name"\`; Remotes []string \`toml:"remotes,omitempty"\` }` near `RemoteConfig`/`SyncPairConfig` types.
    3) Add `func MergeWorkstream(base, override WorkstreamConfig) WorkstreamConfig` using same non-zero-wins style as `MergeRemote`/`MergeSyncPair`; `Remotes` override wins only when `len(override.Remotes) > 0`.
    4) Add `func (c *WorkspaceConfig) WorkstreamByName(name string) (WorkstreamConfig, bool)` that scans `c.Workstreams` and returns first match.

    Respect locked decisions in implementation contract:
    - D-04: only `name` and `remotes` fields on workstream schema.
    - D-13/D-14: no ordering guarantees in this task; sorting occurs in loader (Plan 06-02).
  </action>
  <acceptance_criteria>
    - `pkg/config/config.go` contains `type WorkstreamConfig struct`.
    - `pkg/config/config.go` contains `Workstreams  []WorkstreamConfig` on `WorkspaceConfig`.
    - `pkg/config/config.go` contains `func MergeWorkstream(base, override WorkstreamConfig) WorkstreamConfig`.
    - `pkg/config/config.go` contains `func (c *WorkspaceConfig) WorkstreamByName(name string) (WorkstreamConfig, bool)`.
  </acceptance_criteria>
  <verify>
    <automated>mage testfast</automated>
  </verify>
  <done>Workstream schema contract exists and compiles with existing config package tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add config-level tests for merge and lookup behavior</name>
  <files>pkg/config/config_test.go</files>
  <read_first>
    - pkg/config/config_test.go
    - pkg/config/config.go
    - .planning/phases/06-add-workstream-root-config-block/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Merge default behavior preserves base fields.
    - Test 2: Merge with override remotes replaces base remotes.
    - Test 3: `WorkstreamByName` finds existing and misses unknown names.
  </behavior>
  <action>
    Add table-driven tests:
    1) `TestMergeWorkstream` with cases for empty override, name override, remotes override, nil/empty remotes fallback.
    2) `TestWorkstreamByName` with cases: found first entry, found second entry, missing returns false, duplicate names return first match.

    Align checks with D-05 and D-06 semantics:
    - Include case where `Remotes: []string{}` keeps base (missing semantics handled in loader validation).
    - Include explicit empty remotes value case to prepare for D-06 behavior in loader tests.
  </action>
  <acceptance_criteria>
    - `pkg/config/config_test.go` contains `TestMergeWorkstream`.
    - `pkg/config/config_test.go` contains `TestWorkstreamByName`.
    - `mage testfast` passes.
  </acceptance_criteria>
  <verify>
    <automated>mage testfast</automated>
  </verify>
  <done>Unit tests cover workstream merge/lookup behaviors with deterministic table-driven assertions.</done>
</task>

</tasks>

<verification>
Run:
- `mage testfast`
- `mage test`

Both must pass before marking this plan complete.
</verification>

<success_criteria>
`pkg/config` has a complete workstream schema contract (`WorkstreamConfig`, merge, lookup, `WorkspaceConfig.Workstreams`) with automated tests proving merge and accessor behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-workstream-root-config-block/06-01-SUMMARY.md`.
</output>
