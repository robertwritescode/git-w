---
phase: 12-verify-m1-phases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-add-workspace-block/01-VERIFICATION.md
  - .planning/phases/03-enforce-repos-n-path-convention/03-VERIFICATION.md
autonomous: true
requirements:
  - CFG-01
  - CFG-03
  - CFG-11

must_haves:
  truths:
    - "Phase 01 VERIFICATION.md exists with status passed/gaps_found"
    - "Phase 03 VERIFICATION.md exists with status passed/gaps_found"
    - "CFG-01 and CFG-11 requirements coverage assessed in Phase 01 report"
    - "CFG-03 requirement coverage assessed in Phase 03 report"
  artifacts:
    - path: ".planning/phases/01-add-workspace-block/01-VERIFICATION.md"
      provides: "Phase 01 verification report covering CFG-01, CFG-11"
      contains: "status:"
    - path: ".planning/phases/03-enforce-repos-n-path-convention/03-VERIFICATION.md"
      provides: "Phase 03 verification report covering CFG-03"
      contains: "status:"
  key_links:
    - from: ".planning/phases/01-add-workspace-block/01-01-SUMMARY.md"
      to: ".planning/phases/01-add-workspace-block/01-VERIFICATION.md"
      via: "verification reads summary artifacts and verifies against codebase"
      pattern: "VERIFICATION"
    - from: ".planning/phases/03-enforce-repos-n-path-convention/03-01-SUMMARY.md"
      to: ".planning/phases/03-enforce-repos-n-path-convention/03-VERIFICATION.md"
      via: "verification reads summary artifacts and verifies against codebase"
      pattern: "VERIFICATION"
---

<objective>
Generate VERIFICATION.md reports for Phase 01 (workspace block) and Phase 03 (repos path convention) by checking each phase's implemented artifacts against its ROADMAP success criteria.

Purpose: M1 phases were implemented but never formally verified. This plan closes that gap for Phases 01 and 03.

Output: Two VERIFICATION.md files documenting the verification status of each phase's goal.
</objective>

<execution_context>
@/Users/robert/code/robertwritescode/git-w/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/robert/code/robertwritescode/git-w/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-add-workspace-block/01-01-SUMMARY.md
@.planning/phases/01-add-workspace-block/01-02-SUMMARY.md
@.planning/phases/01-add-workspace-block/01-03-SUMMARY.md
@.planning/phases/03-enforce-repos-n-path-convention/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 01 — Add [[workspace]] block</name>
  <read_first>
    - .planning/phases/01-add-workspace-block/01-01-SUMMARY.md
    - .planning/phases/01-add-workspace-block/01-02-SUMMARY.md
    - .planning/phases/01-add-workspace-block/01-03-SUMMARY.md
    - .planning/ROADMAP.md (Phase 1 section: success criteria, requirements CFG-01, CFG-11)
    - pkg/config/config.go (WorkspaceBlock, MetarepoConfig, AgenticFrameworks)
    - pkg/agents/framework.go (SpecFramework interface)
    - pkg/agents/registry.go (FrameworkFor, FrameworksFor)
    - pkg/config/loader.go (validateAgenticFrameworks, applyMetarepoDefaults)
    - pkg/config/loader_test.go (TestAgenticFrameworksValidation, TestFullV2ConfigLoad)
  </read_first>
  <action>
    Verify Phase 01 against ROADMAP success criteria:
    1. `[[workspace]]` blocks with name, description, and repos list parse correctly from `.gitw`
    2. `agentic_frameworks` field validates against known framework registry; unknown values produce named error listing valid identifiers
    3. Missing `agentic_frameworks` defaults to `["gsd"]`
    4. Multi-value `agentic_frameworks` slices parse and validate correctly

    **Verification steps:**
    1. Read the three SUMMARY.md files to understand what was implemented
    2. Inspect `pkg/config/config.go` for `WorkspaceBlock` struct with `Name`, `Description`, `Repos []string` fields
    3. Inspect `pkg/agents/` for `SpecFramework` interface and `GSDFramework` implementation
    4. Inspect `pkg/config/loader.go` for `validateAgenticFrameworks` and `applyMetarepoDefaults`
    5. Run: `grep -n "WorkspaceBlock\|Workspaces\|AgenticFrameworks" pkg/config/config.go`
    6. Run: `grep -n "validateAgenticFrameworks\|applyMetarepoDefaults" pkg/config/loader.go`
    7. Run: `grep -n "FrameworkFor\|FrameworksFor\|knownFrameworks" pkg/agents/registry.go`
    8. Run: `mage testfast 2>&1 | tail -10` to confirm tests pass
    9. Run: `grep -n "TestAgenticFrameworksValidation\|TestFullV2ConfigLoad" pkg/config/loader_test.go`

    **Write** `.planning/phases/01-add-workspace-block/01-VERIFICATION.md` using the template at `.opencode/get-shit-done/templates/verification-report.md`.

    Use the plan must_haves from 01-01-PLAN.md, 01-02-PLAN.md, 01-03-PLAN.md as the source of truths and artifacts to verify.

    **Expected status:** `passed` (all three plans have SUMMARY.md with completed implementation; tests pass; all success criteria met per summaries).

    Set frontmatter: `phase: 01-add-workspace-block`, `verified: <today's date>T00:00:00Z`, `status: passed` (or `gaps_found` if issues discovered).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-add-workspace-block/01-VERIFICATION.md && grep "status:" .planning/phases/01-add-workspace-block/01-VERIFICATION.md</automated>
  </verify>
  <done>
    - 01-VERIFICATION.md exists in .planning/phases/01-add-workspace-block/
    - File contains valid frontmatter with status field
    - File covers all 4 ROADMAP success criteria for Phase 01
    - CFG-01 and CFG-11 requirements coverage section present
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 03 — Enforce repos/&lt;n&gt; path convention</name>
  <read_first>
    - .planning/phases/03-enforce-repos-n-path-convention/03-01-SUMMARY.md
    - .planning/ROADMAP.md (Phase 3 section: success criteria, requirement CFG-03)
    - pkg/config/config.go (Warnings field on WorkspaceConfig)
    - pkg/config/loader.go (warnNonConformingRepoPaths function)
    - pkg/config/loader_test.go (TestPathConventionWarnings)
  </read_first>
  <action>
    Verify Phase 03 against ROADMAP success criteria:
    1. Repos with paths not matching `repos/<n>` produce a load-time warning
    2. Warning message includes actionable suggestion to run `git w migrate`
    3. Non-conforming paths do not prevent config loading (warning, not error)

    **Verification steps:**
    1. Read 03-01-SUMMARY.md to understand what was implemented
    2. Run: `grep -n "Warnings\|warnNonConformingRepoPaths" pkg/config/config.go pkg/config/loader.go`
    3. Run: `grep -n "git w migrate\|repos.*convention\|repos/" pkg/config/loader.go`
    4. Run: `grep -n "TestPathConventionWarnings" pkg/config/loader_test.go`
    5. Run: `mage testfast 2>&1 | grep "config\|FAIL"` to confirm tests pass

    **Write** `.planning/phases/03-enforce-repos-n-path-convention/03-VERIFICATION.md` using the template.

    **Expected status:** `passed` — SUMMARY.md confirms all three success criteria were implemented, `mage test` passes.

    Set frontmatter: `phase: 03-enforce-repos-n-path-convention`, `verified: <today's date>T00:00:00Z`, `status: passed` (or `gaps_found` if issues discovered).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-enforce-repos-n-path-convention/03-VERIFICATION.md && grep "status:" .planning/phases/03-enforce-repos-n-path-convention/03-VERIFICATION.md</automated>
  </verify>
  <done>
    - 03-VERIFICATION.md exists in .planning/phases/03-enforce-repos-n-path-convention/
    - File contains valid frontmatter with status field
    - File covers all 3 ROADMAP success criteria for Phase 03
    - CFG-03 requirement coverage section present
  </done>
</task>

</tasks>

<verification>
```bash
test -f .planning/phases/01-add-workspace-block/01-VERIFICATION.md && echo "01 ✓" || echo "01 ✗"
test -f .planning/phases/03-enforce-repos-n-path-convention/03-VERIFICATION.md && echo "03 ✓" || echo "03 ✗"
grep "status:" .planning/phases/01-add-workspace-block/01-VERIFICATION.md
grep "status:" .planning/phases/03-enforce-repos-n-path-convention/03-VERIFICATION.md
```
Both files exist with valid status fields.
</verification>

<success_criteria>
- 01-VERIFICATION.md exists covering CFG-01 and CFG-11
- 03-VERIFICATION.md exists covering CFG-03
- Both files have valid frontmatter (phase, verified, status, score)
- Both cover all ROADMAP success criteria for their respective phase
</success_criteria>

<output>
After completion, create `.planning/phases/12-verify-m1-phases/12-01-SUMMARY.md`
</output>
